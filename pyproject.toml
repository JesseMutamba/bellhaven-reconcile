[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "bellhaven-reconcile"
version = "0.1.0"
description = "Evidence-backed facility ownership reconciliation with explicit review"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
bellhaven = "bellhaven.__main__:main"

[tool.setuptools]
packages = ["bellhaven"]
